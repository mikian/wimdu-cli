#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), *%w[.. lib])

require 'bundler'
begin
  Bundler.require(:default)
rescue LoadError
  exec "bundle", "exec", "alula", *ARGV
end

require 'wimdu/cli'
Wimdu::CLI.start
